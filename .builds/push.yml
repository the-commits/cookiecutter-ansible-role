---
image: alpine/edge
packages:
  - git
sources:
  - https://git.sr.ht:~the-commits/cookiecutter-ansible-role
secrets:
  - e3464eed-b716-485b-873c-a785ef7bf68b
environment:
  repo_name: cookiecutter-ansible-role
  github_username: the-commits
  github_repo: the-commits/cookiecutter-ansible-role
tasks:
  - push: |
      set +x
      cd "${repo_name}"
      git config user.name "the-commits"
      git config user.email "12031896+the-commits@users.noreply.github.com"

      cat <<'NOTICE' | sed 's/^      //' > /tmp/notice.md

      > **Do not open issues or pull requests on GitHub** — the mirror there is
      > [read-only](https://git.sr.ht/~the-commits/cookiecutter-ansible-role).
      > Please use the [sourcehut issue tracker](https://todo.sr.ht/~the-commits/cookiecutter-ansible-role)
      > and send patches to [~the-commits/cookiecutter-ansible-role@lists.sr.ht](mailto:~the-commits/cookiecutter-ansible-role@lists.sr.ht).

NOTICE
      sed -i '2r /tmp/notice.md' README.md
      git add README.md
      git commit -m "chore: add mirror notice (GitHub-only)"

      github_token=$(cat ~/secrets/github_pat)
      github_url=$(printf \
        "https://%s:%s@github.com/%s" \
        "${github_username}" \
        "${github_token}" \
        "${github_repo}")
      git push -f "${github_url}" HEAD:main
